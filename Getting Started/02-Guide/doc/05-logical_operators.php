<?php

/**
 * Logical Operators :: Combine two or more than two conditions
 *  - && (AND)
 *  - || (OR)
 *  - !  (NOT)
 */

/**
 * Condition1 Expression && Condition2 Expression :: Both should be true
 * Condition1 Expression || Condition2 Expression :: Either First or Second
 * !Condition :: Return inverse (if condition is true -> Return False) and vise-versa.
 */
